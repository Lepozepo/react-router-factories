## React Router Factories

#### What?
This package explicitly wraps [React Router](https://github.com/reactjs/react-router) components in `React.createFactory`

#### Why?
This makes using `React Router` in `CoffeeScript` easier.
